-- ============================================================
-- 065: 샘플 라인별 메모 컬럼 추가
--
-- 샘플 관리에서 라인별 메모(반납 협의 사항, 거래처 의향 등)를
-- 인라인 편집할 수 있도록 sample_memo 컬럼 추가.
-- ============================================================

ALTER TABLE order_items
  ADD COLUMN IF NOT EXISTS sample_memo TEXT;
