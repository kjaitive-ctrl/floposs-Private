-- ============================================================
-- 149: users.memo 컬럼 추가 — 매장 계정 관리에서 직원 메모 기록용
--
-- 사장 요청 (2026-05-07): 매장계정 추가 폼에 메모 컬럼.
-- (예: "매장 본점", "오전 알바", "퇴사예정 6월" 등 사장 임의 메모)
--
-- 비파괴: nullable TEXT 컬럼 추가만. 기존 행 영향 0.
-- ============================================================

ALTER TABLE users ADD COLUMN IF NOT EXISTS memo TEXT;

NOTIFY pgrst, 'reload schema';
