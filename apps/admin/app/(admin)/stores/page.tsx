"use client";

import { StoresView } from "@/components/StoresView";

// 매장관리 — 슬롯 마스터 + 매장이력 (admin full CRUD)
// [[project_retail_slot_register_ui]] [[project_retail_wholesale_matching_pending]]
export default function StoresPage() {
  return <StoresView />;
}
